# Empty uses the origin serving the application, including on the live domain.
VITE_API_URL=

# Server-side development proxy; this address is never sent to visitors.
BACKEND_PROXY_TARGET=http://127.0.0.1:3001

# Set when exposing the development server through a public reverse proxy.
# DEV_SERVER_PUBLIC_URL=https://librarian.dastari.net
