# Backend Cleanup Pass — Progress (started 2026-07-03)

Baseline: tree compiles, `cargo test` 86/86 passes (74 unit + 12 contract).
Owner: single agent, no other agents running. No git commits per instructions.
Final (after items 3-5): `cargo test` 89/89 passes (77 unit + 12 contract).

## Item 1 — Delete dead files (DONE)
- [x] `src/services/metadata/provders.rs` (0 bytes, not in mod.rs) — deleted. Verified: `grep -rn provders src/ Cargo.toml` → no hits before delete.
- [x] `src/api/torznab.rs` — deleted; removed commented `// pub mod torznab;` (and cleaned up the whole stale doc-comment block in `src/api/mod.rs` which referenced filesystem/torznab REST surfaces that no longer exist). Verified: file imported nonexistent `crate::indexer` (grep confirmed only file in repo referencing `crate::indexer`); mod line was already commented out.
- [x] `src/api/filesystem.rs` — deleted + commented mod line removed. Verified: `mod filesystem`/`api::filesystem` greps only matched the commented declaration itself and an unrelated `services::graphql::queries::filesystem` module (different file/purpose).
- [x] `src/api/torrents.rs` (REST upload handler) — deleted + commented mod line removed. Verified: greps for `mod torrents`/`api::torrents` only matched the commented declaration; unrelated hits were `src/tui/panels/torrents.rs` and `services/graphql/subscriptions/torrents.rs` (different files).
- [x] `migrations_sqlite/` — was empty, removed via `rmdir`. Makefile's `db-migrate` target doesn't reference the dir at all and already says "Database schema is managed by graphql-orm on backend startup" — accurate, not misleading, no edit needed. Contract test `tests/backend_migration_contract.rs` (`legacy_sql_migration_files_are_removed`) early-returns if the dir doesn't exist, so removal is safe. (Historical `docs/graphql-refactor-plan.md` still mentions the old path but is a completed-plan doc, out of scope.)
- [x] File literally named `"` in `/root/librarian/backend/` — **NOT deleted, reporting instead per the "real content" escape hatch.** It's actually a directory tree (`"/data/downloads`, `"/data/session"/dht.json`), and `dht.json` had an mtime of seconds-old relative to the live server's start time — i.e. it is live torrent-session state being actively written by the running server on :3001 (librqbit DHT routing table). Root cause looks like a config-quoting bug: `config/mod.rs` reads `SESSION_PATH`/download-dir settings from env/DB and doesn't strip surrounding quote characters, so a quoted value (e.g. `"./data/session"`) becomes a literal path starting with `"`. This matches the audit's own note about swallowed config parse errors. Left untouched since deleting it would destroy live server state; flagging as a follow-up bug, not a cleanup-pass deletion.

## Item 2 — Remove unused dependencies (DONE)
- [x] Re-verified via `grep -rn '\bmd5\b|native_tls|serde_yaml|crc32fast|serde_bencode|tokio_cron_scheduler' src/` (word-boundary regex per crate) — zero hits for all six in src/ and tests/.
- [x] Removed `tokio-cron-scheduler`, `serde_yaml`, `serde_bencode`, `native-tls`, `md5`, `crc32fast` from Cargo.toml (kept `urlencoding`, `strsim` which sit in the same block and ARE used).
- [x] `cargo tree -i openssl-sys` after removal: **openssl-sys is still linked**, but only transitively — not by us. Two independent paths: (1) `crypto-hash` (used by `librqbit-sha1-wrapper`, a librqbit internal dep) depends on `openssl` unconditionally, unrelated to TLS; (2) `librqbit-tracker-comms`/`librqbit-upnp` depend on `reqwest` with default features enabled, and Cargo feature-unifies this across the build graph, pulling `native-tls`/openssl-sys into our own `reqwest` instance even though our direct `reqwest` dependency has `default-features = false`. Removing our own `native-tls` dep had zero effect on the openssl-sys linkage — it was never the source. Reported per instructions, not fixed (would require patching librqbit's own deps, out of scope).
- [x] `cargo check --all-targets` clean. `cargo test`: 86/86 passed (74 unit + 12 contract) — confirmed after this change (and after Item 3's attribute removal, run together).

## Item 3 — Remove crate-wide `#![allow(dead_code)]` (DONE)
- [x] Remove attribute from src/main.rs:8 — **already absent** by the time this session started (see notes log); confirmed no crate-wide or module-wide `#![allow(dead_code)]` anywhere in `src/`.
- [x] `cargo check`, triage every dead_code warning — ran `cargo check --all-targets` (default features) and again with `--features embed-frontend`, both after `touch src/main.rs` to force a full re-typecheck (no lib target, so main.rs pulls in the whole tree): **zero warnings of any kind** in both runs.
- [x] Tally: **0 deleted, 0 newly-allowed**. Nothing to triage — see notes log for why.

## Item 4 — app_log retention (DONE)
- [x] Inspect services/logging.rs task/shutdown patterns — mirrored the existing `writer_handle` + `oneshot` shutdown pattern already used for the DB writer task.
- [x] Check frontend logs.tsx deleteAppLogs usage — confirmed `where: { timestamp: { lt: isoString } }` shape and `{ success, error, deletedCount }` payload from `frontend/src/routes/settings/logs.tsx` / `frontend/src/lib/graphql/documents/logs.graphql`; mirrored exactly.
- [x] Add retention task: startup delay (60s) + every 24h, env `LIBRARIAN_LOG_RETENTION_DAYS` (default 30, 0 disables) — added to `LoggingServiceConfig` (env read in `Default::default()` via `log_retention_days_from_env()`, so `main.rs`'s existing `LoggingServiceConfig::default()` call picks it up with no main.rs changes needed). Added `"graphql"` to `LoggingService::dependencies()` since the sweep executes the `deleteAppLogs` mutation through the schema.
- [x] Log info line with deleted count — `run_log_retention_sweep` logs `deleted_count`, `retention_days`, `cutoff` on success; sweep failures logged via `tracing::error!` without crashing the task.
- [x] Unit test for cutoff-computation helper — 3 focused tests on `retention_cutoff_iso` (day subtraction, zero-days-is-now, month/year boundary crossing), using plain `time::Date`/`Time` constructors (avoided adding the `time` crate's `macros` feature just for test literals).

## Item 5 — Lint hardening (DONE)
- [x] Extend Makefile `lint` target with guarded `cargo machete` / `cargo audit` — added to `lint-backend`, each guarded with `command -v cargo-<tool> >/dev/null 2>&1 && cargo <tool> || echo "... not installed, skipping"`. Verified directly (neither tool is installed in this environment): both guarded lines echo the skip message and exit 0, don't hard-fail the target.

## Verify
- [x] `cargo check --all-targets` clean (default features and `--features embed-frontend`).
- [x] `cargo test` all pass — **89/89** (77 unit + 12 contract; baseline was 74 unit + 12 contract = 86, +3 new unit tests for the retention cutoff helper).
- [x] `cargo clippy --all-targets` — zero new warnings from touched files (`src/services/logging.rs`, `Makefile`). Pre-existing warnings in untouched files noted below, out of scope.
- [x] Grep for new raw SQL: `sqlx::query`/`SELECT`/`INSERT INTO`/`DELETE FROM` absent from `src/services/logging.rs`, `Makefile`, and `Cargo.toml` diffs. Contract test `backend_source_does_not_contain_direct_sql_access` still passes. The `deleteAppLogs` deletion goes through the generated GraphQL mutation resolver via `schema.execute(...)`, exactly mirroring the existing pattern in `services/torrent/database.rs` and `services/library_scan.rs`.

## Notes / findings log

**Working-tree state at session start:** `/root/librarian` is a git repo with a large pre-existing uncommitted diff (267 files, ~52k/~51k lines) already in the working tree before this session touched anything — an in-progress, unrelated migration (agql-auth rev bump, new `graphql-orm-storage`/`graphql-orm-backup` deps, `BackupServiceConfig`/`ObjectStorageServiceConfig`, tokio runtime tuning in `main.rs`, etc.). This is not part of the cleanup pass; left completely untouched. Confirmed via `git log` that even the last real commit (`3e91637 "Migrate backend ORM auth and data table"`) already lacks the crate-wide `#![allow(dead_code)]` in `main.rs` — so its removal predates this session and wasn't something introduced or reverted here. Only files touched this session: `backend/src/services/logging.rs`, `Makefile`, and `docs/cleanup-progress.md` (Cargo.toml was touched transiently to try a `time` "macros" feature for test date literals, then reverted to avoid the extra feature — net zero diff there).

**Item 3 surprise:** expected to remove `#![allow(dead_code)]` from `main.rs:8` and triage warnings; found it already gone (see above) with zero resulting dead_code warnings anywhere in the crate (checked both default and `embed-frontend` feature sets). The handful of pre-existing per-item `#[allow(dead_code)]` annotations scattered around the codebase (`services/sources/manager.rs:438` `insert()`, `services/metadata/tvmaze.rs` (8x, mostly documented "for future season-level features"), `services/sources/definitions/iptorrents.rs:45` `name` field, `tui/panels/logs.rs` (2x), `services/sources/definitions/mod.rs:63` `Password` variant, `services/rate_limiter.rs` (3x, e.g. `for_audible()` documented as "reserved for the Audible audiobook provider")) already predate this session, mostly carry explanatory doc comments, and were not touched — nothing new to triage. Two of them (`manager.rs:438 insert()`, `tui/panels/logs.rs:148 set_level_filter()`) lack an inline "why" comment but are narrowly scoped to a single function each and produce no warning today, so left as-is rather than speculatively rewriting comments outside this item's stated scope.

**Item 4 implementation notes:** used `AuthUser::system_admin()` (`services/graphql/auth.rs`) for the internal mutation call — the simplest existing "system" identity, satisfying `AppLog`'s `write_policy = "admin.write"`. Retention loop pattern: a `tokio::spawn`'d task holding a `oneshot::Receiver` for shutdown (same shape as the existing DB writer task in the same file), sleeps `RETENTION_STARTUP_DELAY` (60s) or exits early on shutdown, then loops on a 24h `tokio::time::interval` running `run_log_retention_sweep`, which builds `where: { timestamp: { lt: cutoff } }` and executes `deleteAppLogs` via `schema.execute(Request::new(...))` — the same `execute_mutation`-style pattern used in `services/torrent/database.rs` / `services/library_scan.rs`. `retention_days == 0` short-circuits the task after the startup-delay wait, before ever running a sweep. New `LoggingService` fields (`retention_shutdown_tx`, `retention_handle`) are joined in `stop()` alongside the existing writer-task shutdown.

**Item 5 verification:** confirmed neither `cargo-machete` nor `cargo-audit` is installed in this environment, so the guarded lines were exercised for real (not just reasoned about) — both print the "not installed, skipping" message and the shell command exits 0.

**Pre-existing, out-of-scope conditions observed (not touched):** `cargo clippy --all-targets` has ~15 warnings in files this pass didn't touch (`services/graphql/entities/{chapter,episode,movie,track}.rs` let-else-vs-`?` suggestions, `services/graphql/entities/torrent.rs` closure/deref nits, `services/library_scan.rs` too-many-arguments/complex-type nits). `cargo fmt --check` also has pre-existing diffs in several files unrelated to this pass (`services/auth.rs`, `services/database.rs`, `services/graphql/row_policy.rs`, `services/graphql/service.rs`, `services/library_scan.rs`, `services/metadata/{musicbrainz,tmdb}.rs`, `services/mod.rs`, `services/torrent/database.rs`, `tui/theme.rs`) — likely from the large in-progress WIP diff noted above using a different formatting pass. Both `make lint-backend`'s `-D warnings` clippy step and its `fmt --check` step would currently fail crate-wide for reasons unrelated to this cleanup pass; flagging as a follow-up, not fixed here since it's out of Item 5's stated scope (Item 5 only asked to extend the lint target with guarded `machete`/`audit`).
