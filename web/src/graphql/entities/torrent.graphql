# Generated by scripts/generate-entity-documents.mjs from schema.graphql. Do not edit.

fragment TorrentFields on Torrent {
  id
  userId
  infoHash
  magnetUri
  name
  state
  progress
  totalBytes
  downloadedBytes
  uploadedBytes
  uploadedBytesTotal
  savePath
  downloadPath
  sourceUrl
  sourceFeedId
  sourceIndexerId
  libraryId
  postProcessStatus
  postProcessError
  processedAt
  episodeId
  movieId
  trackId
  chapterId
  showId
  albumId
  audiobookId
  minimumRatio
  minimumSeedTimeMinutes
  season
  excludedFiles
  addedAt
  createdAt
  updatedAt
  completedAt
}

query EntityTorrentList($where: TorrentWhereInput, $orderBy: [TorrentOrderByInput!], $page: PageInput) {
  torrents(where: $where, orderBy: $orderBy, page: $page) {
    edges {
      node {
        ...TorrentFields
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
  }
}

query EntityTorrentGet($id: String!) {
  torrent(id: $id) {
    ...TorrentFields
  }
}

mutation EntityTorrentCreate($input: CreateTorrentInput!) {
  createTorrent(input: $input) {
    success
    error
    torrent {
      ...TorrentFields
    }
  }
}

mutation EntityTorrentUpdate($id: String!, $input: UpdateTorrentInput!) {
  updateTorrent(id: $id, input: $input) {
    success
    error
    torrent {
      ...TorrentFields
    }
  }
}

mutation EntityTorrentUpdateMany($where: TorrentWhereInput, $input: UpdateTorrentInput!) {
  updateTorrents(where: $where, input: $input) {
    success
    error
    affectedCount
  }
}

mutation EntityTorrentDelete($id: String!) {
  deleteTorrent(id: $id) {
    success
    error
  }
}

mutation EntityTorrentDeleteMany($where: TorrentWhereInput) {
  deleteTorrents(where: $where) {
    success
    error
  }
}
