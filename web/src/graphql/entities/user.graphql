# Generated by scripts/generate-entity-documents.mjs from schema.graphql. Do not edit.

fragment UserFields on User {
  id
  username
  email
  role
  displayName
  avatarUrl
  isActive
  lastLoginAt
  createdAt
  updatedAt
}

query EntityUserList($where: UserWhereInput, $orderBy: [UserOrderByInput!], $page: PageInput) {
  users(where: $where, orderBy: $orderBy, page: $page) {
    edges {
      node {
        ...UserFields
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
  }
}

query EntityUserGet($id: String!) {
  user(id: $id) {
    ...UserFields
  }
}

mutation EntityUserCreate($input: CreateUserInput!) {
  createUser(input: $input) {
    success
    error
    user {
      ...UserFields
    }
  }
}

mutation EntityUserUpdate($id: String!, $input: UpdateUserInput!) {
  updateUser(id: $id, input: $input) {
    success
    error
    user {
      ...UserFields
    }
  }
}

mutation EntityUserUpdateMany($where: UserWhereInput, $input: UpdateUserInput!) {
  updateUsers(where: $where, input: $input) {
    success
    error
    affectedCount
  }
}

mutation EntityUserDelete($id: String!) {
  deleteUser(id: $id) {
    success
    error
  }
}

mutation EntityUserDeleteMany($where: UserWhereInput) {
  deleteUsers(where: $where) {
    success
    error
  }
}

subscription EntityUserChanged($filter: SubscriptionFilterInput) {
  userChanged(filter: $filter) {
    action
    changeKind
    id
    sourceEntity
    sourceId
    path
    user {
      ...UserFields
    }
  }
}
