# Generated by scripts/generate-entity-documents.mjs from schema.graphql. Do not edit.

fragment TorrentFileFields on TorrentFile {
  id
  torrentId
  fileIndex
  filePath
  relativePath
  fileSize
  downloadedBytes
  progress
  mediaFileId
  isExcluded
  createdAt
  updatedAt
}

query EntityTorrentFileList($where: TorrentFileWhereInput, $orderBy: [TorrentFileOrderByInput!], $page: PageInput) {
  torrentFiles(where: $where, orderBy: $orderBy, page: $page) {
    edges {
      node {
        ...TorrentFileFields
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
  }
}

query EntityTorrentFileGet($id: String!) {
  torrentFile(id: $id) {
    ...TorrentFileFields
  }
}

mutation EntityTorrentFileCreate($input: CreateTorrentFileInput!) {
  createTorrentFile(input: $input) {
    success
    error
    torrentFile {
      ...TorrentFileFields
    }
  }
}

mutation EntityTorrentFileUpdate($id: String!, $input: UpdateTorrentFileInput!) {
  updateTorrentFile(id: $id, input: $input) {
    success
    error
    torrentFile {
      ...TorrentFileFields
    }
  }
}

mutation EntityTorrentFileUpdateMany($where: TorrentFileWhereInput, $input: UpdateTorrentFileInput!) {
  updateTorrentFiles(where: $where, input: $input) {
    success
    error
    affectedCount
  }
}

mutation EntityTorrentFileDelete($id: String!) {
  deleteTorrentFile(id: $id) {
    success
    error
  }
}

mutation EntityTorrentFileDeleteMany($where: TorrentFileWhereInput) {
  deleteTorrentFiles(where: $where) {
    success
    error
  }
}

subscription EntityTorrentFileChanged($filter: SubscriptionFilterInput) {
  torrentFileChanged(filter: $filter) {
    action
    changeKind
    id
    sourceEntity
    sourceId
    path
    torrentFile {
      ...TorrentFileFields
    }
  }
}
