# Generated by scripts/generate-entity-documents.mjs from schema.graphql. Do not edit.

fragment QualityProfileFields on QualityProfile {
  id
  name
  mediaKind
  allowedResolutions
  allowedVideoCodecs
  allowedAudioFormats
  allowedHdrTypes
  allowedSources
  releaseGroupBlacklist
  releaseGroupWhitelist
  requireHdr
  cutoffResolution
  upgradeUntilCutoff
  preferredLanguages
  requireLanguageMatch
  minSizeMb
  maxSizeMb
  minSeeders
  maxReleaseAgeDays
  preferredReleaseGroups
  allowSeasonPacks
  preferProperRepack
  resolutionPreference
  isDefault
  createdAt
  updatedAt
}

query EntityQualityProfileList($where: QualityProfileWhereInput, $orderBy: [QualityProfileOrderByInput!], $page: PageInput) {
  qualityProfiles(where: $where, orderBy: $orderBy, page: $page) {
    edges {
      node {
        ...QualityProfileFields
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      totalCount
    }
  }
}

query EntityQualityProfileGet($id: String!) {
  qualityProfile(id: $id) {
    ...QualityProfileFields
  }
}

mutation EntityQualityProfileCreate($input: CreateQualityProfileInput!) {
  createQualityProfile(input: $input) {
    success
    error
    qualityProfile {
      ...QualityProfileFields
    }
  }
}

mutation EntityQualityProfileUpdate($id: String!, $input: UpdateQualityProfileInput!) {
  updateQualityProfile(id: $id, input: $input) {
    success
    error
    qualityProfile {
      ...QualityProfileFields
    }
  }
}

mutation EntityQualityProfileUpdateMany($where: QualityProfileWhereInput, $input: UpdateQualityProfileInput!) {
  updateQualityProfiles(where: $where, input: $input) {
    success
    error
    affectedCount
  }
}

mutation EntityQualityProfileDelete($id: String!) {
  deleteQualityProfile(id: $id) {
    success
    error
  }
}

mutation EntityQualityProfileDeleteMany($where: QualityProfileWhereInput) {
  deleteQualityProfiles(where: $where) {
    success
    error
  }
}

subscription EntityQualityProfileChanged($filter: SubscriptionFilterInput) {
  qualityProfileChanged(filter: $filter) {
    action
    changeKind
    id
    sourceEntity
    sourceId
    path
    qualityProfile {
      ...QualityProfileFields
    }
  }
}
